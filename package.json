{
  "name": "transaction",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph auth --product hosted-service 43764ac885d243b994ef461366b0b069 smoothbot/user-transactions-fantom",
    "create-local": "graph create --node http://localhost:8020/ transaction",
    "remove-local": "graph remove --node http://localhost:8020/ transaction",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 transaction",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
